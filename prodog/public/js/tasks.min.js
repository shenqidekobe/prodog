let time=5000;
var interval=null;
function executeTask(callback){
	interval=window.setInterval(callback,time);
}
function clearTask(){
	if(interval==null)return;
	window.clearInterval(interval);
}