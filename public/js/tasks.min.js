var task={
	time:5000,
	interval=null,
	executeTask:function(callback){
		task.interval=window.setInterval(callback,task.time);
	},
	clearTask:function(){
		if(task.interval==null)return;
		window.clearInterval(task.interval);
	}
}
